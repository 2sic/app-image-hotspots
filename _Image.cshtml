@inherits ToSic.Sxc.Dnn.RazorComponent

<div @Edit.TagToolbar(Content)>
    <img src="@Content.Image">
</div>

<hr>
<h2>Todo</h2>
Find best way to put the markers on the image. Very likely using JS, because otherwise the image size/responsive will cause positioning problems? Then generate the coordinates etc. from the data